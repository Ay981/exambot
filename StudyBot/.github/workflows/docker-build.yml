name: Build Docker image

on:
  push:
    branches: [ main, master ]
    paths:
      - 'StudyBot/**'
  pull_request:
    paths:
      - 'StudyBot/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: StudyBot
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: StudyBot
          push: false
          platforms: linux/amd64
          tags: studybot:ci-${{ github.sha }}

      # Optional: push to GHCR (enable by setting permissions & variables)
      # - name: Log in to GHCR
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      # - name: Build and push
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: StudyBot
      #     push: true
      #     platforms: linux/amd64
      #     tags: ghcr.io/${{ github.repository_owner }}/studybot:${{ github.sha }}
