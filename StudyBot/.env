# Copy this file to .env and fill in your real keys
GEMINI_API_KEY=AIzaSyANUCjmFaxZ3ehfG9GfpODM4fp9-IMt2MM
TELEGRAM_TOKEN=7492534542:AAEeZxu0SXBPn5fJmUeHMosje7gLQJZOrCU
# Optional: set model name (gemini-1.5-flash is fast; gemini-1.5-pro is stronger)
GEMINI_MODEL=gemini-1.5-flash
# Optional: default page limit to extract (set 0 for all)
PDF_MAX_PAGES=200

# --- Hosting / Webhook settings ---
# When hosting behind a public URL (e.g., Cloud Run/Render/Railway), enable webhook mode:
# USE_WEBHOOK=true
# Base public URL of your service (no trailing slash), e.g., https://your-service-abc.run.app
# WEBHOOK_URL_BASE=
# Path for webhook endpoint (leading slash optional). Change it to something secret-ish.
# WEBHOOK_PATH=/tg-webhook
# The host & port to bind the local server (platform usually injects PORT).
# HOST=0.0.0.0
# PORT=8080
